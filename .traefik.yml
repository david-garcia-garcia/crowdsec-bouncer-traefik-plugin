displayName: CrowdSec Bouncer Traefik Plugin (david-garcia-garcia)
type: middleware
iconPath: .assets/icon.png

import: github.com/david-garcia-garcia/crowdsec-bouncer-traefik-plugin

summary: Middleware plugin which forwards the request IP to local Crowdsec agent, which can be used to allow/deny the request  

testData:
  BouncerEnabled: false

