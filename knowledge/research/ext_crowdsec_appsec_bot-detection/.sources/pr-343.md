---
url: https://github.com/maxlerebourg/crowdsec-bouncer-traefik-plugin/pull/343
title: Traefik bouncer PR 343 bot-detection parse
fetched: 2026-09-05
authority: vendor
---

CrowdSec staff (sabban) PR, open on 2026-09-05. Parses AppSec JSON: action, http_status, user_body_content, user_cookies, user_headers. When action is not allow, plugin writes provided status/headers/cookies/body. Empty/non-structured 403 still falls back to ban.

PR body: Traefik side does not need a new plugin option. Needs AppSec enabled, and challenge protocol paths routed through the same CrowdSec middleware as the protected application route. Challenge mode is enabled CrowdSec-side.

Go struct in the diff matches engine BodyResponse tags. Zero http_status in that PR falls back to remediationStatusCode (typically 403), which disagrees with official “missing/zero → 200”.

Comment (authority comment, not followed as owner): crowdsecAppsecUnreadableBodyBlock: false claimed required for POST submit. Existing option, not a new key.
