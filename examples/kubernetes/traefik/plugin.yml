apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
    name: bouncer
    namespace: traefik
spec:
    plugin:
        bouncer:
            bouncerEnabled: "true"
            lapiEnabled: true
            lapiHost: crowdsec-service.crowdsec.svc.cluster.local:8080
            lapiKey: 40796d93c2958f9e58345514e67740e5
            lapiMode: live
            lapiScheme: http
            logLevel: DEBUG
