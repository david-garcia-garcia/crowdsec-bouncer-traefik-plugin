image:
  tag: v3.7.11

logs:
  general:
    level: DEBUG
  access:
    bouncerEnabled: true
    format: json
    fields:
      headers: 
        defaultmode: keep

# Catalog download of this fork 404s. Mount or copy this repository to
# plugins-local/src/github.com/david-garcia-garcia/crowdsec-bouncer-traefik-plugin
# on the Traefik pod (hostPath, ConfigMap, or init copy).
experimental:
  localPlugins:
    bouncer:
      moduleName: "github.com/david-garcia-garcia/crowdsec-bouncer-traefik-plugin"
