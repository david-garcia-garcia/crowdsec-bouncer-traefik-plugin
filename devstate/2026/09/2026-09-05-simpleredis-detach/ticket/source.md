# Detach pkg/simpleredis from upstream

Remove `pkg/simpleredis/LICENSE` and `pkg/simpleredis/SOURCE` and assume throughout the codebase and docs that we now own this piece of code and there is no upstream for it.

Use a dedicated worktree.
