LiveLookup caches a header-scope ban on the client-address cache slot (`pkg/lapi/client_live.go:43-46`) so other clients behind that IP inherit the ban. Header results are already stored on HeaderScopeKey (`pkg/lapi/client_decisions.go:142,150-160`). Proven FAIL on origin/master: TestHunt_LiveLookupDoesNotCacheHeaderBanOnIP. Fix: write only the IP query result to the IP key; keep header remediations on HeaderScopeKey. A later cache lookup for the same IP and a different header value must not inherit another identity's ban. Include a regression test. Bound the ask to this defect only.
