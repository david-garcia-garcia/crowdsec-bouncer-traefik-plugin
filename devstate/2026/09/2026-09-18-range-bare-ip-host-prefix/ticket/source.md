Bare-IP Range decisions are stored then ignored (`pkg/decisionscope/range.go:29-34` upserts any non-empty string; rangemembership.go:35-37 AddCIDR fails and skips). Trusted-pool hostCIDR already maps bare IP to /32 or /128 (`pkg/ip/checker.go:76-80`). Proven FAIL: TestHunt_RangeBareIPIsHostPrefix. Fix: treat a parseable bare IP as /32 or /128 so a Range host value remediates that address. Include a regression test. Bound the ask to this defect only.
