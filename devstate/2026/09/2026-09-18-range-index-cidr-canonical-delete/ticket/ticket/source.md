Range-index upsert/delete match CIDR text, not the network (`pkg/decisionscope/range.go:68,94-108`). After AddRange(10.1.2.0/8), RemoveRange(10.0.0.0/8) leaves 10.1.2.3 banned. Proven FAIL: TestHunt_RemoveRangeEquivalentCIDRSpelling. Fix: upsert and remove Range lines by canonical net.IPNet (network address + prefix length). Include a regression test. Do not take #77/#34 IP cache-key work. Bound the ask to this defect only.
