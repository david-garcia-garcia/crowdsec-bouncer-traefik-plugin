validateLogging opens LogFilePath and discards the handle (`pkg/configuration/configuration.go:484-488`). plugin.New already opened the same path via logger.NewWithFormat. Proven FAIL: TestHunt_ValidateParams_closesLogFileAfterWritabilityCheck (Windows Remove fails file-in-use). Fix: close the file opened for the writability check (or reuse the logger handle) so successful ValidateParams does not retain an extra descriptor. Include a regression test. Bound the ask to this defect only. Do not take logger-file-reclaim debt unless required.
