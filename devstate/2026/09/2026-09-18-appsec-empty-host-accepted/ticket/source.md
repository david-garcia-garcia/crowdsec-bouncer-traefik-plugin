ValidateParams accepts crowdsecAppsecEnabled:true with empty CrowdsecAppsecHost because validateURL treats http:/// as valid (`pkg/configuration/configuration.go:511-517,455-458`). Proven FAIL: TestHunt_ValidateParams_rejectsEmptyAppsecHostWhenEnabled. Fix: when AppSec is enabled, reject an empty crowdsecAppsecHost (and any AppSec URL that http.NewRequest accepts only because the host is missing). Default AppSec failure action is ban, so this currently starts 'healthy' then blocks the site. Include a regression test. Bound the ask to this defect only.
