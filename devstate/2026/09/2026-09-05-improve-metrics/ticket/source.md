# Improve CrowdSec LAPI usage metrics

The metrics endpoint for CrowdSec can receive more information, such as the scenario. Need better metrics, sliced by scenario plus any other things that the CrowdSec LAPI is able to ingest.

Pointer: `pkg/crowdsecconnection/connection.go` `reportMetrics` (end of file).
