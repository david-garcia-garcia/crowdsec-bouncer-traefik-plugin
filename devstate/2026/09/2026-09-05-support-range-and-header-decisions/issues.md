# Issues

- [ ] note large  range-index linear scan → radix tree (or a bounded walk)
  Why: explore deferred a local radix tree. Stream/alone still walk every `cidr=remediation` line per request. Risk: public contract and Redis-sharing shape stay one blob.
