A CDN in front of Traefik cached the captcha HTML this plugin serves (HTTP 200 on the original URL, Content-Type only, no Set-Cookie). After the captcha decision was cleared, the CDN kept serving that stored page. Fix: set Cache-Control: no-cache, no-store on the captcha challenge response written by Client.ServeHTTP. Set the same Cache-Control header on the ban page written by handleBanServeHTTP. Match the header CrowdSec already uses on the HAProxy bouncer captcha/ban returns and on the AppSec challenge protocol example (no-cache, no-store). Do not change AppSec challenge relay (it already copies user_headers, including Cache-Control from the engine).
