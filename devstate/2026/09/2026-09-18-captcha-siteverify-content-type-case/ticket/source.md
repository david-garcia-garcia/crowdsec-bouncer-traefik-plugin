Siteverify Content-Type match is case-sensitive (`pkg/captcha/captcha.go:324-327` strings.HasPrefix application/json). Application/JSON + {"success":true} is treated as non-JSON (200 challenge, no cookie). RFC 9110 media types are case-insensitive. Proven FAIL: TestHunt_siteverifyJSONContentTypeIsCaseInsensitive. Fix: treat siteverify as JSON when the media type (before parameters) equals application/json case-insensitively; success:true must issue the gate cookie and 302. Not #52. Include a regression test. Bound the ask to this defect only.
