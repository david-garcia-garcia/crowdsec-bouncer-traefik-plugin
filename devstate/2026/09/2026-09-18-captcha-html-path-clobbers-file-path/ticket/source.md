plugin.New aliases captchaHtmlFilePath onto captchaFilePath whenever the deprecated key is non-empty (`plugin.go:31-32`), unlike ban which fills only when the current key is empty (`plugin.go:28-29`). Proven FAIL: TestHunt_captchaFilePathWinsOverDeprecatedHTMLPath. Fix: alias captchaHtmlFilePath onto captchaFilePath only when captchaFilePath is empty; when both are set, compile and serve captchaFilePath. Include a regression test. Bound the ask to this defect only.
