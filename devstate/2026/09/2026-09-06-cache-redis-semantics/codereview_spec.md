# Spec

- [completed] TTL-zero, error return, read-your-writes, Redis CRUD tests — all in change spec delta and landed in code.
  Status: completed
  Argument: Matches `core_cache_redis_in-tree-client` delta.
