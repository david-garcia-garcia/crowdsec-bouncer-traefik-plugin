# Test coverage

1. [judgement] Edge case untested — `openspec/changes/.../core_plugin_lapi_scope-union/spec.md` — scenario "Subscriber does not add scopes" has no dedicated test; behavior follows because subscribers never Open
   → Optional stream integration test if scope union regresses
   Status: skipped
   Argument: architectural guarantee; existing OpenStream union tests unchanged.
