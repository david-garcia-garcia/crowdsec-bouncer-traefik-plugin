# Spec

1. [wrong] Named slot request path — `(spec)` — change and live `core_plugin_middleware_named-instance` required MUST NOT store `*Client` from construct while `publishedLAPI` keeps a construct fallback per deviations.md
   → Align the SHALL with Peek-first plus construct fallback for Openers; subscribers use Peek and failure actions
   Status: done
   Argument: updated change and live named-instance spec requirement text.
