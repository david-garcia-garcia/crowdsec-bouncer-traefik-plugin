getToken builds the CAPI login body with fmt.Sprintf (`pkg/lapi/client_http.go:165-170`), so a quote or backslash in machine_id, password, or a scenario yields invalid JSON. Proven FAIL: TestHunt_GetTokenLoginBodyIsValidJSON. Fix: marshal a struct (machine_id, password, scenarios) with encoding/json so any legal CAPI credential produces a decodable body and posted fields match config. Include a regression test. Bound the ask to this defect only.
