# Issues

- [ ] note large  custom provider JSON siteverify (upstream #318) → shared JSON verify for `custom`
  Why: TryCap needs JSON siteverify; `custom` still PostForms. Sharing that encoding would also unblock CapJS/`custom` operators. Not taken: requirement.md out of scope; this change only adds `trycap`.
