setGateCookie sets Secure only when r.TLS != nil (`pkg/captcha/gate.go:74-86`). Traefik behind Cloudflare/ALB has TLS==nil and X-Forwarded-Proto: https; the grace cookie is then sent on a same-host HTTP entrypoint. Proven FAIL: TestHunt_gateCookieSecureWhenForwardedProtoHTTPS. Fix: set Secure when r.TLS != nil OR the request is client-HTTPS via X-Forwarded-Proto from a hop the bouncer already trusts (same trusted-IP / insecure-forwarded model as GetRemoteIP). Include a regression test. Bound the ask to this defect only.
