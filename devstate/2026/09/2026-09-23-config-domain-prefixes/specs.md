# Specs
change: config-domain-prefixes

FindSpecHost (Search then verdict, one per delta). Live catalog: remaining public-key and timeout contracts are live promises. No skip.

Search: `openspec/specs/map.md` families `core_plugin_middleware`, `core_plugin_lapi`, `core_plugin_appsec`, `core_plugin_decisionstore`, `build_e2e_pester`. Walked `openspec/specs/*/spec.md` and `openspec/changes/config-domain-prefixes/specs/` (empty at Search). Explore fold list accepted. Identity-owner Decision: reuse `pkg/lapi/identity.go` and `pkg/appsec/session.go` marshalers.

- fold `core_plugin_middleware_config-validation` confidence: high candidates: [core_plugin_middleware_config-validation]
- fold `core_plugin_middleware_bouncer` confidence: high candidates: [core_plugin_middleware_bouncer]
- fold `core_plugin_lapi_connection` confidence: high candidates: [core_plugin_lapi_connection]
- fold `core_plugin_lapi_reclaim-key` confidence: high candidates: [core_plugin_lapi_reclaim-key]
- fold `core_plugin_lapi_scope-union` confidence: high candidates: [core_plugin_lapi_scope-union]
- fold `core_plugin_lapi_failure-action` confidence: high candidates: [core_plugin_lapi_failure-action]
- fold `core_plugin_appsec_client` confidence: high candidates: [core_plugin_appsec_client]
- fold `core_plugin_appsec_failure-action` confidence: high candidates: [core_plugin_appsec_failure-action]
- fold `core_plugin_appsec_bot-detection` confidence: high candidates: [core_plugin_appsec_bot-detection]
- fold `core_plugin_decisionstore_store` confidence: high candidates: [core_plugin_decisionstore_store]
- fold `build_e2e_pester_crowdsec-stack` confidence: high candidates: [build_e2e_pester_crowdsec-stack]

Rename volume across more than three requirements is name-only on those leaves, not a new capability. Explore Decision: no new spec family. `proposal.md` New vs Modified matches (New none, Modified the eleven folds).
