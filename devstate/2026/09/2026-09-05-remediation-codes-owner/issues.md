# Issues

- [ ] note large  stream lease key `updated` uses `NoBannedValue` (`f`) as dummy payload
  Why: Get only checks presence; the value is not a remediation. Not taken: ticket keeps wire bytes and forbids extra packages.
