# Issues

- [ ] note large  spec `core_plugin_ip_radix-lookup` → a leaf that names hop-trust files / GetRemoteIP
  Why: family `core_plugin_ip` is right; leaf `radix-lookup` names the Checker tree, not GetRemoteIP file isolation. Risk: existing cites and archive folders. Not taken this run.
