# Standards

1. [hard] Name for the scope — `openspec/specs/core_plugin_lapi_reclaim-key/spec.md:8` — `Visitor address` is a second domain name for the GetRemoteIP role; the dump and siblings (`core_plugin_ip_radix-lookup`, `core_plugin_middleware.md`) use `client address` / `client IP`
   → Rename to `client address`
   Status: done
   Argument: Renamed `Visitor address` to `Client address` in live and change copies of `core_plugin_lapi_reclaim-key/spec.md`.
