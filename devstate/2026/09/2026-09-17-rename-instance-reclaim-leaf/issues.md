# Issues

- [ ] take large  `openspec/specs/core_plugin_middleware_instance-reclaim` → precise legal 4th part or split (FindSpecHost in propose)
  Why: leaf `instance-reclaim` hides session prefix, settings hash, transport adopt, and Bouncer policy. Human approved.
