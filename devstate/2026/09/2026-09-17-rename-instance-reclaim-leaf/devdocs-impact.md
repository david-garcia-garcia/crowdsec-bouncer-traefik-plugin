# Devdocs impact
change: rename-instance-reclaim-leaf

## Units
- LAPI reclaim key — subsystem — `openspec/specs/core_plugin_lapi_reclaim-key`
- Bouncer — subsystem — `openspec/specs/core_plugin_middleware_bouncer` / `knowledge/devdocs/core_plugin_middleware.md`
- LAPI connection — subsystem — `openspec/specs/core_plugin_lapi_connection` / `knowledge/devdocs/core_plugin_lapi_connection.md`
- LAPI failure action — subsystem — `openspec/specs/core_plugin_lapi_failure-action` (fold; no body rewrite)

## Findings
- [x] missing-packet  LAPI reclaim key — no packet whose heading is this unit; only How-to/Gotcha plus spec cites on `core_plugin_middleware` and `core_plugin_lapi_connection`
