# Security

1. [judgement] Secrets in logs — `pkg/lapi/client_decisions.go:139` — the promoted `WARN` line embeds `headerErr.Error()`, and a transport failure from `sendQuery` formats as `crowdsecQuery:unreachable url:<full URL including query>`. That query string carries `value=<request header value>`, i.e. operator-mapped client input (`username`, `Country`), so a request header value now reaches the log at the plugin's default level instead of `DEBUG`.
   → Log host plus scope name and drop the query string from the wrapped LAPI error if header values must not appear in operator logs
   Status: skipped
   Argument: judgement, and the ticket asks for exactly this level. Confirmed the three gates: source is the mapped request header (client-controlled), sink is the operator log, but the blast radius is a same-trust-boundary log entry, not a credential — the LAPI API key travels in the `Apikey` header and the CAPI token in `Authorization`, never in the URL, so no secret is in the string. Log injection is closed off separately: `url.QueryEscape` at `pkg/lapi/client_decisions.go:137` percent-encodes CR/LF before the value ever reaches the URL. This is a visibility change to a line that already logged the same text at `DEBUG` on `0e7dbf0`, not a new source-to-sink edge. Noted next to the existing `knowledge/debt/2026-09-18-trace-header-untrusted-value.md`.

Fail-open on deny: this diff is the fix for one. `pkg/lapi/client_live.go:44` turns the previous allow-on-scope-error into a non-active remediation plus an error, which `pkg/bouncer/bouncer.go:233-239` routes into `applyLapiFailureAction` (default `ban`), and `:47-49` no longer caches the unverified allow, so an outage cannot outlive itself in the cache. No new fail-open was introduced: every new early return on the live path returns either an active remediation or `("", err)`.

Other rows, all clean: no secret, credential, or `.env` added; no README or fixture token; no new trust of a header as identity (the scopes map is the operator's existing `decisionScopeHeaders`); no new competing identity calculation; no path or URL built from config; no new outbound host (`sendQuery` reuses `currentTransport()` from #62 and its `HTTPTimeoutSeconds`); no header written onto a request or response; no concatenation into a shell, SQL, or template; no TLS-verify change and no new download; nothing new on a client-visible response or header — the reverse-proxy message at `pkg/lapi/client_http.go:235` stays in the process error path; no new dependency.