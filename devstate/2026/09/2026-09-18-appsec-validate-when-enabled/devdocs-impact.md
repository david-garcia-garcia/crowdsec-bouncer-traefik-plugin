# Devdocs impact
change: appsec-validate-when-enabled

## Units
- Config validation — subsystem — `knowledge/devdocs/core_plugin_middleware_config-validation.md` / Requirement: AppSec URL key and HTTPS CA validated only when enabled

## Findings
- [x] stale-usage  Config validation — How-to, Gotchas, and Key files are captcha-only; they do not name the `AppsecEnabled` gate around `validateAppsecURLKeyAndTLS`
