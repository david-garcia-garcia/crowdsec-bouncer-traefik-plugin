getToken requires login.Code == 200 AND a token (`pkg/lapi/client_http.go:181-193`). CrowdSec WatcherAuthResponse marks code omitempty; official apiclient uses Token after HTTP 2xx. A 2xx body {"token":"fresh","expire":"later"} returns getToken statusCode:0. Proven FAIL: TestHunt_GetTokenAcceptsTwoXXTokenWithoutJSONCode. Fix: after a 2xx CAPI login, store login.Token when non-empty; do not require code==200. Keep the existing error when the token is empty. Include a regression test. Bound the ask to this defect only (do not also rewrite login JSON unless required for the test).
